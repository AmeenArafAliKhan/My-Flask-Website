from market import application

if __name__=='__main__': #checks if run.py is executed directly
    application.run(debug=True)  #runs the Flask file and sets up necessary envoronment variables.

